With jquery.animate_numerically you can easily show your users an animated count.

This is useful in games or other apps with a lot changing numbers.

Use like this:

    $('#weight').animateNumerically(80, 70, function() {
      return n + ' kg';
    });
